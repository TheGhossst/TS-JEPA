# Experiment overlay: diagnostic / future training data at 10 ms observation cadence.
_base: ts_jepa_baseline.yaml

simulation:
  sampling_interval_ms: 10

paths:
  data_root: data_sampling_10ms
  runs_root: runs
  ts_jepa_dirname: ts_jepa_sampling_10ms
  semantic_actor_dirname: semantic_actor_sampling_10ms
