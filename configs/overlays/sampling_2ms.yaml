# Experiment overlay: diagnostic / future training data at 2 ms observation cadence.
_base: ts_jepa_baseline.yaml

simulation:
  sampling_interval_ms: 2

paths:
  data_root: data_sampling_2ms
  runs_root: runs
  ts_jepa_dirname: ts_jepa_sampling_2ms
  semantic_actor_dirname: semantic_actor_sampling_2ms
